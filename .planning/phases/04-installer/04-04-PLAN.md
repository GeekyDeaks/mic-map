---
phase: 04-installer
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - installer/MicMap.iss
  - installer/micmap.ico
  - apps/micmap/micmap.rc
autonomous: true
requirements: [INST-01]
must_haves:
  truths:
    - "installer/MicMap.iss exists and compiles with ISCC when a valid STAGE_DIR is provided"
    - "AppId is a stable forever-frozen GUID that enables Inno Setup's upgrade-in-place semantics"
    - "Architectures are locked to x64os (NOT deprecated x64) — ARM64 Windows refuses to install"
    - "All [Files] Source paths reference {#STAGE_DIR}\\* — zero repo-relative paths (which would only work on the dev machine)"
    - "Wizard is welcome → progress → finished, with DisableDirPage=yes since the install dir is SteamVR-derived"
  artifacts:
    - path: "installer/MicMap.iss"
      provides: "Inno Setup script [Setup] preamble + [Files] + icon wiring + placeholder for later Pascal sections"
      contains: "AppId={{BC6D91A7-A852-4562-8CBF-58FC4662FEDC}"
      min_lines: 60
    - path: "installer/micmap.ico"
      provides: "Installer icon (also reused by micmap.rc / tray)"
  key_links:
    - from: "installer/MicMap.iss"
      to: "{#STAGE_DIR}\\drivers\\micmap\\*"
      via: "[Files] Source"
      pattern: "Source:.*STAGE_DIR"
    - from: "installer/MicMap.iss"
      to: "installer/micmap.ico"
      via: "SetupIconFile directive"
      pattern: "SetupIconFile"
---

<objective>
Create the initial `installer/MicMap.iss` Inno Setup script with the `[Setup]` preamble (AppId, version, architecture, privileges, wizard), the `[Files]` block pulling from `{#STAGE_DIR}`, `#define` guards for the CMake `/D` defines, and the installer icon. This is the skeleton — Plans 05-08 add the Pascal `[Code]` sections (InitializeSetup, PrepareToInstall, CurStepChanged, CurUninstallStepChanged) on top of this foundation.

**Stable AppId GUID (frozen forever per INST-01):** `BC6D91A7-A852-4562-8CBF-58FC4662FEDC`. This GUID was generated specifically for MicMap v1.0 at plan time (2026-04-23). It MUST NOT be regenerated or changed — Inno Setup uses it to detect same-app upgrade scenarios. A different GUID = new-install semantics, old version stays in Add/Remove Programs.

Purpose: establish the scaffolding that later Pascal sections hang off; prove the `.iss` compiles under ISCC with just the stage contents (no orchestration yet).
Output: `installer/MicMap.iss` (Setup + Files + Icon + version preprocessor guards), `installer/micmap.ico` (new asset), updated `apps/micmap/micmap.rc` pointing at the new shared `.ico`.
</objective>

<execution_context>
@$HOME/.ccs/instances/bigscreen/get-shit-done/workflows/execute-plan.md
@$HOME/.ccs/instances/bigscreen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-installer/04-CONTEXT.md
@.planning/phases/04-installer/04-RESEARCH.md
@.planning/phases/04-installer/04-PATTERNS.md
@.planning/phases/04-installer/04-VALIDATION.md
@.planning/phases/04-installer/04-03-SUMMARY.md

@apps/micmap/micmap.rc
@apps/micmap/CMakeLists.txt
@CMakeLists.txt

<interfaces>
Verbatim [Setup] block template (04-RESEARCH.md §Open Question 8, ADJUSTED for D-17 x64os fix):
```
[Setup]
AppId={{BC6D91A7-A852-4562-8CBF-58FC4662FEDC}
AppName=MicMap
AppVersion={#MICMAP_VERSION}
AppPublisher=MicMap
DefaultDirName={code:GetMicMapInstallDir}  ; Plan 05 adds GetMicMapInstallDir
DisableDirPage=yes
DisableWelcomePage=no
DisableProgramGroupPage=yes
WizardStyle=modern
PrivilegesRequired=admin
ArchitecturesAllowed=x64os              ; D-17 — x64 deprecated in IS 6.3+
ArchitecturesInstallIn64BitMode=x64os
Uninstallable=yes
OutputDir={#OUTPUT_DIR}
OutputBaseFilename=MicMap-Setup-v{#MICMAP_VERSION}
UsePreviousAppDir=yes
UsePreviousSetupType=yes
UsePreviousTasks=yes
CloseApplications=no                    ; WE handle closing via PrepareToInstall
SetupIconFile=micmap.ico
UninstallDisplayIcon={app}\bin\micmap.exe
```

[Files] block pattern (04-RESEARCH.md §Open Question 3 + D-01 nested layout + D-06 restartreplace on DLLs):
```
[Files]
Source: "{#STAGE_DIR}\drivers\micmap\bin\win64\driver_micmap.dll"; \
  DestDir: "{app}\bin\win64"; Flags: ignoreversion restartreplace uninsrestartdelete
Source: "{#STAGE_DIR}\drivers\micmap\driver.vrdrivermanifest"; \
  DestDir: "{app}"; Flags: ignoreversion
Source: "{#STAGE_DIR}\drivers\micmap\resources\*"; \
  DestDir: "{app}\resources"; Flags: ignoreversion recursesubdirs createallsubdirs
Source: "{#STAGE_DIR}\bin\micmap.exe"; \
  DestDir: "{app}\bin"; Flags: ignoreversion
Source: "{#STAGE_DIR}\bin\app.vrmanifest"; \
  DestDir: "{app}\bin"; Flags: ignoreversion
```

#define preprocessor guards (04-RESEARCH.md §Open Question 3 top of file):
```
#ifndef MICMAP_VERSION
  #define MICMAP_VERSION "0.0.0-dev"
#endif
#ifndef STAGE_DIR
  #error "STAGE_DIR must be defined via /DSTAGE_DIR=... (see CMakeLists.txt package target)"
#endif
#ifndef OUTPUT_DIR
  #define OUTPUT_DIR "."
#endif
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate/source installer/micmap.ico and wire it into apps/micmap/micmap.rc</name>
  <files>installer/micmap.ico, apps/micmap/micmap.rc</files>
  <read_first>
    - apps/micmap/micmap.rc (entire file — note the commented-out `// IDI_MICMAP_ICON ICON "micmap.ico"` slot at line ~10-11)
    - .planning/phases/04-installer/04-PATTERNS.md §"NEW installer/micmap.ico" (planner discretion guidance)
    - .planning/phases/04-installer/04-RESEARCH.md §"Open Questions" point 3 (recommendation: single icon shared by exe + installer + tray)
    - .planning/phases/04-installer/04-CONTEXT.md §decisions "Claude's Discretion" (installer icon reuse)
  </read_first>
  <behavior>
    - One `.ico` file used by BOTH the tray/exe and the installer — single visual identity (04-RESEARCH.md Open Q #3 recommendation)
    - File placed at `installer/micmap.ico` (the canonical location referenced by the .iss `SetupIconFile=` directive)
    - `apps/micmap/micmap.rc` updated to reference the same icon file via relative path (e.g., `"../../installer/micmap.ico"`) so both the exe and installer share one .ico source
    - If no existing `.ico` can be sourced, generate a simple placeholder icon (a multi-resolution .ico with at least 16×16, 32×32, 48×48, 256×256 frames) — we need SOMETHING present for ISCC to compile against
    - This task is explicitly for the **file asset** — the .iss SetupIconFile directive is wired in Task 2
  </behavior>
  <action>
    1. Check for any existing `.ico` in the repo: `find . -name '*.ico' 2>/dev/null | head -5`. If one exists in `apps/`, `resources/`, or similar, copy it to `installer/micmap.ico`.

    2. If no existing `.ico` found: generate a placeholder. Use PowerShell or an image library to produce a multi-res `.ico`. A simple approach — if ImageMagick is available on the dev box: `magick -background transparent -size 256x256 xc:none -fill "#4A90E2" -draw "circle 128,128 128,50" installer/micmap.ico`. If no ImageMagick: use any minimal known-good `.ico` bitstream (even a 16×16 single-frame white square works for compile-pass; polish deferred to discretion). The file MUST exist and be a valid `.ico` for ISCC to accept it.

    3. Modify `apps/micmap/micmap.rc` — locate the commented slot around line 10-11 (`// If you want a custom icon...`) and uncomment the ICON directive with the relative path. Per CONVENTIONS / CLAUDE.md Windows paths, use forward slashes or escaped backslashes; Windows resource compiler accepts both:
       ```
       IDI_MICMAP_ICON ICON "../../installer/micmap.ico"
       ```
       (Adjust the relative path based on the actual `.rc` file's location. Typical: `apps/micmap/micmap.rc` → `../../installer/micmap.ico`.)
  </action>
  <verify>
    <automated>test -f installer/micmap.ico &amp;&amp; grep -q 'IDI_MICMAP_ICON ICON' apps/micmap/micmap.rc &amp;&amp; grep -q 'micmap.ico' apps/micmap/micmap.rc &amp;&amp; ! grep -q '^// IDI_MICMAP_ICON' apps/micmap/micmap.rc &amp;&amp; cmake --build build --config Release --target micmap 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test -f installer/micmap.ico` exits 0
    - File size > 0 bytes (a real .ico, not an empty file): `test -s installer/micmap.ico`
    - `apps/micmap/micmap.rc` contains an uncommented `IDI_MICMAP_ICON ICON "..micmap.ico"` (or equivalent path) line
    - `cmake --build build --config Release --target micmap` exits 0 — resource compiler accepts the new icon reference
  </acceptance_criteria>
  <done>installer/micmap.ico exists and is a valid ICO file. micmap.rc references the same icon via relative path. micmap.exe compiles with the new icon resource embedded (visible via `build/Release/micmap.exe` → right-click Properties → icon preview on Windows).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create installer/MicMap.iss with [Setup] preamble + [Files] + preprocessor guards</name>
  <files>installer/MicMap.iss</files>
  <read_first>
    - .planning/phases/04-installer/04-PATTERNS.md §"NEW installer/MicMap.iss" (full — section skeleton overview)
    - .planning/phases/04-installer/04-RESEARCH.md §"Executive Summary" point 1 (x64os correction) and §"Open Question 3" (preprocessor guards) and §"Open Question 4" (restartreplace on DLL) and §"Open Question 8" (verbatim [Setup] block)
    - .planning/phases/04-installer/04-VALIDATION.md tasks 04-04-01 / 04-04-02 / 04-04-03 (acceptance greps)
    - .planning/phases/04-installer/04-CONTEXT.md §decisions D-01 (nested layout), D-14 (WizardStyle=modern, DisableDirPage=yes), D-15 (no license), D-16 (no code-signing), D-17 (x64), D-20 (OutputDir), D-21 (/D defines)
  </read_first>
  <behavior>
    - Stable AppId **`BC6D91A7-A852-4562-8CBF-58FC4662FEDC`** hardcoded in `[Setup]` (frozen forever — generated at plan time, per INST-01 and 04-CONTEXT.md Claude's Discretion)
    - Architecture tokens are **`x64os`** (NOT `x64` — deprecated per IS 6.3+, flagged in 04-RESEARCH.md Executive Summary point 1 and 04-VALIDATION.md 04-04-01)
    - `DefaultDirName={code:GetMicMapInstallDir}` — the Pascal function is added in Plan 05; for now this references a symbol that doesn't exist yet. **Deviation:** to make Plan 04 compilable in isolation, define a minimal stub `[Code]` block at the end of the .iss:
      ```pascal
      [Code]
      function GetMicMapInstallDir(Param: String): String;
      begin
          // Plan 05 replaces this with HKCU\Software\Valve\Steam\SteamPath resolution
          Result := ExpandConstant('{autopf}\MicMap');  // placeholder — never reached in prod
      end;
      ```
      Plan 05 replaces the body with the real registry-lookup implementation. This keeps Plan 04's .iss compile-green on its own.
    - `[Files]` uses `{#STAGE_DIR}\*` throughout — zero repo-relative paths (04-VALIDATION.md 04-04-03)
    - `driver_micmap.dll` gets `restartreplace uninsrestartdelete` flags (04-RESEARCH.md §Open Question 4)
    - `CloseApplications=no` so Inno's built-in process-kill UI doesn't fire (we handle gating in Plan 06's PrepareToInstall)
    - `[Run]` and `[UninstallRun]` sections deliberately omitted — Plan 07 adds the orchestration via `CurStepChanged(ssPostInstall)` + `CurUninstallStepChanged(usUninstall)` (research §Open Question 9 Technique B)
  </behavior>
  <action>
    1. Create `installer/MicMap.iss` with the full content below (organized by section, with comments marking Plan-05..08 extension points):

       ```
       ; MicMap Installer — Inno Setup 6.7.1 script
       ; Phase 4 INST-01..INST-08 (see .planning/phases/04-installer/)
       ; AppId frozen 2026-04-23 — DO NOT regenerate (Inno upgrade-in-place depends on it)

       ; ---------------------------------------------------------------
       ; Preprocessor guards (set by CMake `package` target via /D defines)
       ; ---------------------------------------------------------------
       #ifndef MICMAP_VERSION
         #define MICMAP_VERSION "0.0.0-dev"
       #endif
       #ifndef STAGE_DIR
         #error "STAGE_DIR must be defined via /DSTAGE_DIR=... (see CMakeLists.txt package target)"
       #endif
       #ifndef OUTPUT_DIR
         #define OUTPUT_DIR "."
       #endif

       [Setup]
       AppId={{BC6D91A7-A852-4562-8CBF-58FC4662FEDC}
       AppName=MicMap
       AppVersion={#MICMAP_VERSION}
       AppPublisher=MicMap
       AppPublisherURL=https://github.com/mic-map
       DefaultDirName={code:GetMicMapInstallDir}
       DisableDirPage=yes
       DisableWelcomePage=no
       DisableProgramGroupPage=yes
       WizardStyle=modern
       PrivilegesRequired=admin
       ; D-17 — x64os (NOT deprecated x64 per IS 6.3+; refuses ARM64 explicitly)
       ArchitecturesAllowed=x64os
       ArchitecturesInstallIn64BitMode=x64os
       Uninstallable=yes
       OutputDir={#OUTPUT_DIR}
       OutputBaseFilename=MicMap-Setup-v{#MICMAP_VERSION}
       UsePreviousAppDir=yes
       UsePreviousSetupType=yes
       UsePreviousTasks=yes
       ; WE handle closing-SteamVR in PrepareToInstall (Plan 06) — disable Inno's built-in UI
       CloseApplications=no
       SetupIconFile=micmap.ico
       UninstallDisplayIcon={app}\bin\micmap.exe

       [Languages]
       Name: "english"; MessagesFile: "compiler:Default.isl"

       [Files]
       ; D-01 nested layout: {app} == {SteamVR}\drivers\micmap
       ; Driver DLL at {app}\bin\win64 (matches driver/CMakeLists.txt install RUNTIME DESTINATION)
       ; Pitfall 4 (D-06 defense-in-depth): restartreplace + uninsrestartdelete on the DLL
       Source: "{#STAGE_DIR}\drivers\micmap\bin\win64\driver_micmap.dll"; \
         DestDir: "{app}\bin\win64"; \
         Flags: ignoreversion restartreplace uninsrestartdelete

       Source: "{#STAGE_DIR}\drivers\micmap\driver.vrdrivermanifest"; \
         DestDir: "{app}"; \
         Flags: ignoreversion

       Source: "{#STAGE_DIR}\drivers\micmap\resources\*"; \
         DestDir: "{app}\resources"; \
         Flags: ignoreversion recursesubdirs createallsubdirs

       ; App binaries at {app}\bin (D-03)
       Source: "{#STAGE_DIR}\bin\micmap.exe"; \
         DestDir: "{app}\bin"; \
         Flags: ignoreversion

       Source: "{#STAGE_DIR}\bin\app.vrmanifest"; \
         DestDir: "{app}\bin"; \
         Flags: ignoreversion

       ; ---------------------------------------------------------------
       ; [Run] / [UninstallRun] intentionally OMITTED.
       ; Plan 07 adds orchestration via CurStepChanged(ssPostInstall) + Exec()
       ; per 04-RESEARCH.md §Open Question 9 Technique B (ResultCode inspection).
       ; ---------------------------------------------------------------

       [Code]
       ; ---------------------------------------------------------------
       ; Plan 04 stub — replaced by Plan 05 with HKCU\Software\Valve\Steam
       ; registry resolution (D-02) and Plan 06 (WMI SteamVR-running gate).
       ; ---------------------------------------------------------------
       function GetMicMapInstallDir(Param: String): String;
       begin
           // Placeholder — Plan 05 overwrites with real implementation.
           Result := ExpandConstant('{autopf}\MicMap');
       end;
       ```

    2. Confirm via syntax check that the `.iss` is well-formed (ISCC will check on compile; grep-only linting for now since installer/ is brand-new).
  </action>
  <verify>
    <automated>test -f installer/MicMap.iss &amp;&amp; grep -q 'AppId={{BC6D91A7-A852-4562-8CBF-58FC4662FEDC}' installer/MicMap.iss &amp;&amp; grep -q 'ArchitecturesAllowed=x64os' installer/MicMap.iss &amp;&amp; ! grep -E 'ArchitecturesAllowed=x64($|[^o])' installer/MicMap.iss &amp;&amp; grep -q 'PrivilegesRequired=admin' installer/MicMap.iss &amp;&amp; grep -q 'Uninstallable=yes' installer/MicMap.iss &amp;&amp; grep -q 'SetupIconFile=micmap.ico' installer/MicMap.iss &amp;&amp; grep -q '{#STAGE_DIR}' installer/MicMap.iss &amp;&amp; grep -q 'restartreplace' installer/MicMap.iss &amp;&amp; ! grep -E 'Source:[^{]*\.\./' installer/MicMap.iss &amp;&amp; grep -q '#ifndef STAGE_DIR' installer/MicMap.iss</automated>
  </verify>
  <acceptance_criteria>
    - `test -f installer/MicMap.iss` exits 0
    - `grep -q 'AppId={{BC6D91A7-A852-4562-8CBF-58FC4662FEDC}' installer/MicMap.iss` exits 0 — stable AppId frozen (04-VALIDATION 04-04-02)
    - `grep -q 'ArchitecturesAllowed=x64os' installer/MicMap.iss` exits 0 AND `! grep -qE 'ArchitecturesAllowed=x64($|[^o])' installer/MicMap.iss` — x64os only, no bare deprecated x64 (04-VALIDATION 04-04-01)
    - `grep -q 'PrivilegesRequired=admin' installer/MicMap.iss` exits 0 (04-VALIDATION 04-04-02)
    - `grep -q 'Uninstallable=yes' installer/MicMap.iss` exits 0
    - `grep -q 'SetupIconFile=micmap.ico' installer/MicMap.iss` exits 0
    - `grep -q '{#STAGE_DIR}' installer/MicMap.iss` exits 0 (all [Files] paths use stage)
    - `! grep -E 'Source:[^{]*\.\./' installer/MicMap.iss` exits 0 — zero repo-relative paths (04-VALIDATION 04-04-03)
    - `grep -q 'restartreplace' installer/MicMap.iss` exits 0 (Pitfall 4 defense-in-depth)
    - **ISCC compile smoke:** If `ISCC_EXECUTABLE` is resolvable, running `ISCC /DMICMAP_VERSION=0.0.0-test /DSTAGE_DIR=build/stage /DOUTPUT_DIR=build/installer installer/MicMap.iss` exits 0 (stage must already exist from Plan 03 — verified). ISCC exits 2 on compile errors; exit 0 = well-formed .iss. If ISCC not present on dev box, this sub-check is skipped with a note in the SUMMARY.
  </acceptance_criteria>
  <done>`installer/MicMap.iss` is a compilable Inno Setup 6.7.1 script with [Setup], [Languages], [Files], and a stub [Code] section. AppId frozen to `BC6D91A7-A852-4562-8CBF-58FC4662FEDC`. Architectures locked to x64os. [Files] sources from {#STAGE_DIR}. Wave 3 baseline ready for Plans 05-08 to add Pascal callbacks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer process ↔ file system | Admin-elevated installer writes to `{SteamVR}\drivers\micmap\` and subsidiaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Spoofing | Unsigned installer | accept | v1.0 ships unsigned per D-16; SmartScreen warning acceptable for indie launch. Code-signing revisited in v1.x. |
| T-04-04 | Tampering | [Files] Source path injection | mitigate | All sources are `{#STAGE_DIR}\*` literal paths — no runtime interpolation, no user-supplied path segments. STAGE_DIR comes from the dev's CMake invocation (trusted). |
</threat_model>

<verification>
Full plan green when:
- `installer/micmap.ico` exists and is a valid ICO
- `installer/MicMap.iss` passes all grep acceptance criteria (AppId frozen, x64os set, no repo-relative Source paths, SetupIconFile present)
- `micmap.exe` still compiles with the new .ico resource reference
- Optional: ISCC smoke compile exits 0 against the Plan 03 stage dir
</verification>

<success_criteria>
- AppId `BC6D91A7-A852-4562-8CBF-58FC4662FEDC` frozen in .iss — enables Inno's upgrade-in-place semantics (INST-01 + D-01)
- x64os architecture tokens (IS 6.3+ compliant) — ARM64 Windows refuses install (D-17)
- PrivilegesRequired=admin + Uninstallable=yes (INST-01 + INST-05 preambles)
- [Files] pulls from {#STAGE_DIR} — single source of truth via Plan 03's install() rules
- restartreplace + uninsrestartdelete on driver_micmap.dll (Pitfall 4 defense-in-depth for INST-02)
- Shared icon asset at installer/micmap.ico used by both the .iss SetupIconFile and micmap.rc IDI_MICMAP_ICON
</success_criteria>

<output>
After completion, create `.planning/phases/04-installer/04-04-SUMMARY.md` capturing:
- Stable AppId GUID chosen: `BC6D91A7-A852-4562-8CBF-58FC4662FEDC` — FROZEN FOREVER
- Icon source (existing `.ico` reused vs. generated placeholder)
- ISCC smoke-compile result if ISCC was available on the dev box
- Plan 05 hand-off: GetMicMapInstallDir stub body is placeholder — needs replacement with HKCU registry lookup
</output>
