---
phase: 04-installer
plan: 09
type: execute
wave: 8
depends_on: [08]
files_modified:
  - scripts/install_driver.bat
  - scripts/uninstall_driver.bat
  - scripts/install_driver_test.bat
  - scripts/test_driver.bat
  - installer/MicMap.iss
  - build/installer/MicMap-Setup-vX.Y.Z.exe
autonomous: true
requirements: [INST-07]
must_haves:
  truths:
    - "Obsolete batch scripts (install_driver.bat, uninstall_driver.bat, install_driver_test.bat, test_driver.bat) no longer exist in the repo — the single .exe installer replaces them per CONTEXT.md <domain>"
    - "`cmake --build build --target package --config Release` runs end-to-end (clean configure → stage → ISCC compile) and produces build/installer/MicMap-Setup-v{PROJECT_VERSION}.exe"
    - "ISCC.exe exits 0 when compiling installer/MicMap.iss with all Plan 05/06/07/08 Pascal callbacks composed — full cross-plan integration smoke"
    - "The produced .exe is non-trivial in size (> 1MB sanity floor) — proves the stage tree + embedded Pascal Script + icon are actually packaged, not an empty shell"
    - "ROADMAP.md Success Criterion #5 (one build command yields one shippable installer) is satisfied at the file-exists level"
  artifacts:
    - path: "build/installer/MicMap-Setup-v*.exe"
      provides: "Shippable MicMap installer produced end-to-end by `cmake --build --target package`"
      contains: "(binary — size > 1MB)"
  key_links:
    - from: "cmake --build build --target package"
      to: "build/installer/MicMap-Setup-vX.Y.Z.exe"
      via: "Plan 03 add_custom_target(package) → cmake --install → ISCC.exe"
      pattern: "MicMap-Setup-v.*\\.exe"
    - from: "installer/MicMap.iss"
      to: "ISCC.exe compile"
      via: "iscc invocation in package target (verbatim smoke re-run in Task C)"
      pattern: "ISCC_EXECUTABLE"
---

<objective>
End-to-end close-out for Phase 4: delete the obsolete batch scripts, then exercise the full `cmake --build --target package` pipeline to prove Plans 01-08 compose into one shippable installer.

This plan fills the gap between Plan 03 (which defined the `package` target but deferred end-to-end exercise because `installer/MicMap.iss` did not yet exist) and ROADMAP.md Phase 4 Success Criterion #5 ("`cmake --build --target package` produces `MicMap-Setup-vX.Y.Z.exe` via ISCC.exe with correct `/D` defines — one build command yields one shippable installer").

**Three tasks:**

1. **Delete batch scripts** — `scripts/install_driver.bat`, `scripts/uninstall_driver.bat`, `scripts/install_driver_test.bat`, `scripts/test_driver.bat` per CONTEXT.md `<domain>` boundary ("Deletion of scripts/install_driver.bat, scripts/uninstall_driver.bat, scripts/install_driver_test.bat, scripts/test_driver.bat (replaced by the single .exe installer)") and 04-VALIDATION.md row 04-09-01.
2. **End-to-end package build** — `cmake --build build --target package --config Release` from a clean configure. Produces `build/installer/MicMap-Setup-vX.Y.Z.exe`. This is the first moment in Phase 4 where all Plan 01-08 outputs are assembled by one command.
3. **Optional: ISCC standalone smoke re-run** — `iscc /Qp` directly on `installer/MicMap.iss` after all Plan 05-08 Pascal callbacks have composed; a belt-and-braces check that ISCC's own exit code is 0 and the compile is not a false positive from the CMake wrapping.

Closes **INST-07** at the end-to-end level (Plan 03 closed it at the build-machinery level; this plan closes it at the produces-an-actual-.exe level). Also discharges the CONTEXT.md `<domain>` deliverable "Deletion of scripts/install_driver*.bat" which no earlier plan addresses.

Purpose: final wave of Phase 4 — the shippable artifact exists on disk, and the repo no longer contains the batch scripts that the installer replaces.
Output: four batch scripts deleted; `build/installer/MicMap-Setup-vX.Y.Z.exe` on disk and > 1MB.
</objective>

<execution_context>
@$HOME/.ccs/instances/bigscreen/get-shit-done/workflows/execute-plan.md
@$HOME/.ccs/instances/bigscreen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-installer/04-CONTEXT.md
@.planning/phases/04-installer/04-RESEARCH.md
@.planning/phases/04-installer/04-PATTERNS.md
@.planning/phases/04-installer/04-VALIDATION.md
@.planning/phases/04-installer/04-03-PLAN.md
@.planning/phases/04-installer/04-04-PLAN.md
@.planning/phases/04-installer/04-05-PLAN.md
@.planning/phases/04-installer/04-06-PLAN.md
@.planning/phases/04-installer/04-07-PLAN.md
@.planning/phases/04-installer/04-08-PLAN.md

@CMakeLists.txt
@installer/MicMap.iss
@scripts/install_driver.bat
@scripts/uninstall_driver.bat
@scripts/install_driver_test.bat
@scripts/test_driver.bat

<interfaces>
From CONTEXT.md `<domain>` (verbatim deliverable list):
```
- Deletion of `scripts/install_driver.bat`, `scripts/uninstall_driver.bat`,
  `scripts/install_driver_test.bat`, `scripts/test_driver.bat`
  (replaced by the single `.exe` installer).
```

From ROADMAP.md Phase 4 Success Criterion #5 (verbatim):
```
5. `cmake --build --target package` produces `MicMap-Setup-vX.Y.Z.exe`
   via ISCC.exe with correct `/D` defines — one build command yields
   one shippable installer (validates INST-07).
```

From Plan 03 (04-03-PLAN.md Task 2 acceptance note — the deferral being closed here):
```
Note: `cmake --build build --target package` is NOT part of this plan's
acceptance (it would fail — `installer/MicMap.iss` doesn't exist until
Plan 04). That is Plan 10's end-to-end exercise.
```

From 04-VALIDATION.md rows 04-09-01 and 04-09-02 (acceptance commands):
```
04-09-01 | Removed batch scripts no longer referenced by build system or CI |
  `! test -f scripts/install_driver.bat && ! test -f scripts/uninstall_driver.bat
   && ! grep -rE 'install_driver\.bat|uninstall_driver\.bat'
        CMakeLists.txt driver/CMakeLists.txt .github 2>/dev/null`

04-09-02 | `copy_distributable_files` custom target deleted
           (duplicates `cmake --install` — Pitfall 17 per research) |
  `! grep -q 'copy_distributable_files' CMakeLists.txt`
```
(Plan 03 already closed 04-09-02's `copy_distributable_files` deletion as part of its Task 2.)

From CMakeLists.txt (Plan 03-installed target, per 04-03-PLAN.md §"Replace with package target"):
```cmake
add_custom_target(package
    COMMAND ${CMAKE_COMMAND} -E remove_directory "${MICMAP_STAGE_DIR}"
    COMMAND ${CMAKE_COMMAND} -E make_directory   "${MICMAP_STAGE_DIR}"
    COMMAND ${CMAKE_COMMAND} --install "${CMAKE_BINARY_DIR}"
                             --prefix  "${MICMAP_STAGE_DIR}"
                             --config  $<CONFIG>
    COMMAND ${CMAKE_COMMAND} -E make_directory "${MICMAP_INSTALLER_DIR}"
    COMMAND "${ISCC_EXECUTABLE}"
            "/DMICMAP_VERSION=${PROJECT_VERSION}"
            "/DSTAGE_DIR=${MICMAP_STAGE_DIR}"
            "/DOUTPUT_DIR=${MICMAP_INSTALLER_DIR}"
            "${MICMAP_ISS_FILE}"
    ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task A: Delete obsolete batch scripts (CONTEXT.md &lt;domain&gt; deliverable)</name>
  <files>scripts/install_driver.bat, scripts/uninstall_driver.bat, scripts/install_driver_test.bat, scripts/test_driver.bat</files>
  <read_first>
    - scripts/install_driver.bat (confirm the file still exists and is the expected legacy script)
    - scripts/uninstall_driver.bat (same)
    - scripts/install_driver_test.bat (same)
    - scripts/test_driver.bat (same)
    - CMakeLists.txt (confirm Plan 03 already deleted `copy_distributable_files` and no other build rule references the batch scripts)
    - driver/CMakeLists.txt (defensive — confirm no references)
    - README.md (informational — note that README.md:23,38 reference install_driver.bat; Phase 5 DOC-01 updates those, NOT this plan. Deletion here is intentional; the docs will catch up in Phase 5.)
    - .planning/phases/04-installer/04-CONTEXT.md §&lt;domain&gt; (verbatim deliverable list, fourth bullet) and §&lt;code_context&gt; "scripts/ directory" integration-points note
    - .planning/phases/04-installer/04-VALIDATION.md row 04-09-01 (acceptance command)
  </read_first>
  <behavior>
    - All four batch scripts are removed from the repo. `git rm` (preferred) or filesystem delete + `git add -u` both work — prefer `git rm` so the change shows cleanly in `git status`.
    - No build-system file is edited in this task — Plan 03 already deleted the `copy_distributable_files` target (which was the only build rule copying these scripts). If any lingering reference to the batch scripts is found in `CMakeLists.txt` / `driver/CMakeLists.txt` / `.github/`, document it in the SUMMARY and defer to a follow-up; do NOT silently patch files outside the declared `files_modified` list.
    - README.md references (lines ~23, 38) are intentionally NOT touched here — Phase 5 DOC-01 owns the docs update. A transient doc/reality mismatch between Phase 4 close and Phase 5 open is the accepted tradeoff.
  </behavior>
  <action>
    1. From repo root, execute:
       ```
       git rm scripts/install_driver.bat \
              scripts/uninstall_driver.bat \
              scripts/install_driver_test.bat \
              scripts/test_driver.bat
       ```
       (If the files have already been deleted from the filesystem but are still tracked, use `git rm --cached` instead; if fully absent both on disk and in the index, no-op and note in SUMMARY.)

    2. Run 04-VALIDATION.md row 04-09-01's grep sub-command to confirm no lingering build-system / CI references:
       ```
       ! grep -rE 'install_driver\.bat|uninstall_driver\.bat' \
         CMakeLists.txt driver/CMakeLists.txt .github 2>/dev/null
       ```
       Expected exit 0 (no match). If a match is found, record in SUMMARY and escalate — do NOT delete the lingering reference as part of this task (outside declared files_modified).

    3. Do NOT touch `README.md`. Phase 5 DOC-01 owns that update. The doc/reality mismatch between Phase 4 close and Phase 5 open is explicitly accepted per CONTEXT.md `<domain>` out-of-scope item "README / architecture docs referring to the new installer flow → Phase 5 (DOC-01)".
  </action>
  <verify>
    <automated>! test -f scripts/install_driver.bat &amp;&amp; ! test -f scripts/uninstall_driver.bat &amp;&amp; ! test -f scripts/install_driver_test.bat &amp;&amp; ! test -f scripts/test_driver.bat &amp;&amp; ! grep -rE 'install_driver\.bat|uninstall_driver\.bat' CMakeLists.txt driver/CMakeLists.txt 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `! test -f scripts/install_driver.bat` exits 0 — 04-VALIDATION 04-09-01 first clause
    - `! test -f scripts/uninstall_driver.bat` exits 0 — 04-VALIDATION 04-09-01 second clause
    - `! test -f scripts/install_driver_test.bat` exits 0 — full CONTEXT.md `<domain>` coverage
    - `! test -f scripts/test_driver.bat` exits 0 — full CONTEXT.md `<domain>` coverage
    - `! grep -rE 'install_driver\.bat|uninstall_driver\.bat' CMakeLists.txt driver/CMakeLists.txt 2>/dev/null` exits 0 — 04-VALIDATION 04-09-01 grep clause (no build-system reference remains)
    - `git status --porcelain -- scripts/` shows the four deletions (`D  scripts/install_driver.bat` etc.) — sanity that `git rm` actually staged the removals
  </acceptance_criteria>
  <done>Four batch scripts deleted from the repo via `git rm`. CMakeLists.txt / driver/CMakeLists.txt free of references. README.md intentionally not touched (Phase 5 scope). CONTEXT.md `<domain>` batch-script-deletion deliverable closed.</done>
</task>

<task type="auto">
  <name>Task B: End-to-end package build — cmake --build --target package produces MicMap-Setup-vX.Y.Z.exe</name>
  <files>build/installer/MicMap-Setup-vX.Y.Z.exe</files>
  <read_first>
    - CMakeLists.txt (confirm Plan 03's `find_program(ISCC_EXECUTABLE ...)` + `add_custom_target(package ...)` are present — this is the target invoked here)
    - installer/MicMap.iss (confirm it exists — this is the .iss that Plans 04-08 composed)
    - build/stage/ listing if it exists (`find build/stage -type f 2>/dev/null | sort | head -50`) — informational; the package target cleans and re-stages on every invocation
    - apps/micmap/CMakeLists.txt (spot-check `install(FILES app.vrmanifest DESTINATION bin)` is present — Plan 03 Task 1 deliverable; required for the stage tree to be complete)
    - .planning/phases/04-installer/04-CONTEXT.md §decisions D-18 / D-19 / D-20 / D-21 (build machinery decisions — Plan 03 implemented; this task exercises them)
    - .planning/phases/04-installer/04-VALIDATION.md rows 04-03-01 / 04-03-02 / 04-03-03 (acceptance commands for the configure → stage → package chain)
    - .planning/ROADMAP.md §"Phase 4: Installer" Success Criterion #5 (the criterion this task closes)
  </read_first>
  <behavior>
    - Clean configure: `cmake -B build -DCMAKE_BUILD_TYPE=Release` (or re-configure if already configured — either works; the target's first step clears `build/stage` so no stale state matters).
    - Build all normal targets first to satisfy package's `DEPENDS micmap driver_micmap`: `cmake --build build --config Release`. This ensures micmap.exe + driver_micmap.dll exist before ISCC is invoked.
    - Invoke the package target: `cmake --build build --target package --config Release`. This runs (per Plan 03): clear stage → cmake --install into stage → mkdir installer output dir → ISCC.exe with /D defines → echo output path.
    - Expected artifacts:
      - `build/stage/drivers/micmap/bin/win64/driver_micmap.dll` (from driver/CMakeLists.txt install rules)
      - `build/stage/bin/micmap.exe` (from root CMakeLists.txt install(TARGETS micmap ...))
      - `build/stage/bin/app.vrmanifest` (from Plan 03 Task 1's install(FILES ...))
      - `build/installer/MicMap-Setup-v{PROJECT_VERSION}.exe` (from ISCC)
    - ISCC.exe must exit 0. If ISCC is not installed on the dev box, this task cannot complete on this machine — the work must run on a machine with Inno Setup 6.7.1 installed per STACK.md. Record absence in SUMMARY if encountered and escalate.
    - Sanity floor: the produced .exe must be > 1MB. The real stage tree (driver DLL + app EXE + ImGui/D3D11 runtime + Pascal Script + icon + manifest) should produce something in the ~2-8 MB range. A < 1MB result means something is empty — investigate before declaring done.
  </behavior>
  <action>
    1. Clean configure (optional if already configured; harmless if re-run):
       ```
       cmake -B build -DCMAKE_BUILD_TYPE=Release
       ```
       Acceptance: exit 0, `build/CMakeCache.txt` contains `ISCC_EXECUTABLE:FILEPATH=...` pointing at a real ISCC.exe. If ISCC not found (cache var absent or empty), abort with a clear SUMMARY note naming the machine and the missing Inno Setup install.

    2. Build all normal targets (satisfies `DEPENDS micmap driver_micmap`):
       ```
       cmake --build build --config Release
       ```
       Acceptance: exit 0. `build/Release/micmap.exe` (or `build/bin/Release/micmap.exe` depending on generator) and `build/driver/Release/driver_micmap.dll` (or equivalent) exist.

    3. Invoke the package target:
       ```
       cmake --build build --target package --config Release
       ```
       Acceptance: exit 0. The echo step at the end prints the path to `build/installer/MicMap-Setup-v{PROJECT_VERSION}.exe`.

    4. Verify artifact + stage tree:
       ```
       test -f build/stage/drivers/micmap/bin/win64/driver_micmap.dll
       test -f build/stage/bin/micmap.exe
       test -f build/stage/bin/app.vrmanifest
       ls build/installer/MicMap-Setup-v*.exe
       ```
       Acceptance: all four exist. The shell-glob `MicMap-Setup-v*.exe` matches exactly one file (the versioned produced installer). The produced .exe size > 1MB (see verify-block `wc -c`).

    5. Record the produced artifact's absolute path, size, and SHA256 in the SUMMARY for traceability. This is the "end-of-phase-4 shippable artifact" reference point for verify-work and the v1.0 retrospective.
  </action>
  <verify>
    <automated>cmake -B build -DCMAKE_BUILD_TYPE=Release 2>&amp;1 | tail -5 &amp;&amp; grep -q 'ISCC_EXECUTABLE.*=/\|ISCC_EXECUTABLE.*=[A-Za-z]:' build/CMakeCache.txt &amp;&amp; cmake --build build --config Release 2>&amp;1 | tail -10 &amp;&amp; cmake --build build --target package --config Release 2>&amp;1 | tail -20 &amp;&amp; test -f build/stage/drivers/micmap/bin/win64/driver_micmap.dll &amp;&amp; test -f build/stage/bin/micmap.exe &amp;&amp; test -f build/stage/bin/app.vrmanifest &amp;&amp; ls build/installer/MicMap-Setup-v*.exe &amp;&amp; INSTALLER=$(ls build/installer/MicMap-Setup-v*.exe | head -1) &amp;&amp; [ $(wc -c &lt; "$INSTALLER") -gt 1048576 ]</automated>
  </verify>
  <acceptance_criteria>
    - `cmake -B build -DCMAKE_BUILD_TYPE=Release` exits 0 (04-VALIDATION 04-03-01)
    - `grep -q 'ISCC_EXECUTABLE.*=' build/CMakeCache.txt` matches a non-empty path (04-VALIDATION 04-03-01)
    - `cmake --build build --config Release` exits 0 (normal targets build — prerequisite for `DEPENDS micmap driver_micmap`)
    - `cmake --build build --target package --config Release` exits 0 — **Phase 4 Success Criterion #5 at the cmake-level**
    - `test -f build/stage/drivers/micmap/bin/win64/driver_micmap.dll` exits 0 (04-VALIDATION 04-03-02)
    - `test -f build/stage/bin/micmap.exe` exits 0 (04-VALIDATION 04-03-02)
    - `test -f build/stage/bin/app.vrmanifest` exits 0 (04-VALIDATION 04-03-02) — confirms Plan 03 Task 1's install(FILES ...) is wired
    - `ls build/installer/MicMap-Setup-v*.exe` matches exactly one file (04-VALIDATION 04-03-03) — **Phase 4 Success Criterion #5 at the artifact-on-disk level**
    - `[ $(wc -c < build/installer/MicMap-Setup-v*.exe) -gt 1048576 ]` — **sanity floor > 1MB**, proves the stage tree + Pascal Script + icon are actually packaged (not an empty shell)
    - ISCC exit code 0 is implied by the package target exiting 0 (if ISCC failed, cmake would fail and propagate non-zero); this is the canonical signal that Plans 05/06/07/08's composed `[Code]` section compiles
  </acceptance_criteria>
  <done>`build/installer/MicMap-Setup-v{PROJECT_VERSION}.exe` exists on disk, > 1MB, produced by a single `cmake --build --target package` invocation. Stage tree confirmed to contain driver_micmap.dll, micmap.exe, and app.vrmanifest at the D-01 paths. Phase 4 ROADMAP Success Criterion #5 closed at the file-exists level; runtime UAT (clean VM install → detect → toggle → uninstall) is the 04-VALIDATION.md §Manual-Only Verifications checklist and runs outside this plan.</done>
</task>

<task type="auto">
  <name>Task C (optional but recommended): Standalone ISCC smoke re-run on installer/MicMap.iss</name>
  <files></files>
  <read_first>
    - installer/MicMap.iss (confirm all Plan 05-08 callbacks are in place — grep for `InitializeSetup`, `PrepareToInstall`, `CurStepChanged`, `CurUninstallStepChanged`, `SweepLegacyBindings`, `PromptAndMaybeRemoveUserData` as smoke checks)
    - Task B SUMMARY output (informational — the ISCC_EXECUTABLE path resolved by CMake is the same binary this task re-invokes)
    - .planning/phases/04-installer/04-CONTEXT.md §decisions D-21 (the three /D defines to pass)
    - .planning/phases/04-installer/04-06-PLAN.md / 04-07-PLAN.md / 04-08-PLAN.md §acceptance_criteria "ISCC smoke compile (optional)" entries — same invocation pattern used here
  </read_first>
  <behavior>
    - This task is a belt-and-braces re-run of ISCC standalone (not via CMake) to confirm the .iss compiles on its own with the three D-defines. It is OPTIONAL: Task B's successful package build already implies ISCC exited 0. But running ISCC directly provides:
      1. A faster re-compile loop for any follow-up Pascal edits (skips cmake --install re-staging).
      2. A cleanly separated exit code — useful in the SUMMARY for retrospective / troubleshooting.
      3. A chance to use `/Qp` (quiet + progress) which produces more readable console output than the CMake-wrapped invocation.
    - Skip entirely if ISCC is not on PATH or at the Plan 03 fallback locations; record skip in SUMMARY with reason.
    - Stage dir from Task B must still exist (package target leaves `build/stage/` populated after success). If Task B was not run, this task's ISCC invocation will fail because `{#STAGE_DIR}` paths reference missing files.
  </behavior>
  <action>
    1. Resolve ISCC_EXECUTABLE from the CMakeCache:
       ```
       ISCC=$(grep 'ISCC_EXECUTABLE:FILEPATH=' build/CMakeCache.txt | cut -d= -f2)
       ```
       If empty or file not executable, skip this task and record in SUMMARY.

    2. Run ISCC standalone with the three D-defines per D-21:
       ```
       "$ISCC" /Qp \
         "/DMICMAP_VERSION=$(grep 'CMAKE_PROJECT_VERSION:STATIC=' build/CMakeCache.txt | cut -d= -f2)" \
         "/DSTAGE_DIR=$(pwd)/build/stage" \
         "/DOUTPUT_DIR=$(pwd)/build/installer" \
         installer/MicMap.iss
       ```
       Acceptance: exit 0, final line of output names the produced .exe path.

    3. Verify the output .exe timestamp updated (ISCC re-produced the installer) and size still > 1MB:
       ```
       ls -l build/installer/MicMap-Setup-v*.exe
       ```
  </action>
  <verify>
    <automated>ISCC=$(grep 'ISCC_EXECUTABLE:FILEPATH=' build/CMakeCache.txt | cut -d= -f2) &amp;&amp; test -x "$ISCC" &amp;&amp; VER=$(grep 'CMAKE_PROJECT_VERSION:STATIC=' build/CMakeCache.txt | cut -d= -f2) &amp;&amp; "$ISCC" /Qp "/DMICMAP_VERSION=$VER" "/DSTAGE_DIR=$(pwd)/build/stage" "/DOUTPUT_DIR=$(pwd)/build/installer" installer/MicMap.iss &amp;&amp; ls build/installer/MicMap-Setup-v*.exe &amp;&amp; [ $(wc -c &lt; $(ls build/installer/MicMap-Setup-v*.exe | head -1)) -gt 1048576 ]</automated>
  </verify>
  <acceptance_criteria>
    - ISCC_EXECUTABLE resolved from `build/CMakeCache.txt` and points to an existing, executable file
    - `$ISCC /Qp ...` invocation exits 0 — ISCC-side standalone confirmation that `installer/MicMap.iss` (with all Plan 05-08 Pascal callbacks composed) compiles cleanly
    - `ls build/installer/MicMap-Setup-v*.exe` matches exactly one file (re-produced by this run)
    - Produced .exe size > 1MB (same sanity floor as Task B)
    - **Skip path:** if ISCC is unavailable, this task's verify block returns non-zero; SUMMARY must document the skip reason (machine without Inno Setup installed). Plan 09 is NOT considered failed by skipping Task C as long as Task A + Task B are green. Task C is "belt-and-braces" per the revision instructions; Task B alone satisfies Phase 4 Success Criterion #5.
  </acceptance_criteria>
  <done>ISCC standalone smoke compile exits 0 with all Plan 05-08 Pascal callbacks composed; produced .exe size > 1MB. If ISCC unavailable: skip recorded in SUMMARY with reason (and Tasks A + B are still green).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev workspace ↔ git index | `git rm` stages deletions |
| build/stage ↔ ISCC.exe | Staged files packaged into installer — same boundary as Plan 03 |
| dev workspace ↔ build/installer | CMake + ISCC produce artifact — same boundary as Plan 03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | Deleted batch scripts resurrected via `git revert` by a future maintainer who missed CONTEXT.md `<domain>` | accept | Dev-workflow trust model. CONTEXT.md `<domain>` documents the deletion as a deliverable; Phase 5 DOC-01 updates README to remove any lingering references. Future resurrection would be caught at code-review time. |
| T-04-09 | Tampering | build/installer/*.exe produced from a compromised build/stage | accept (inherited from Plan 03 T-04-01) | Dev-machine trust model; code-signing deferred to v1.x per D-16. Same boundary as Plan 03 Task 2's package target. No new surface introduced here. |
</threat_model>

<verification>
Full plan green when:
- Task A verify block exits 0 (all four batch scripts absent; no lingering CMake reference)
- Task B verify block exits 0 (clean configure + full build + package target produce `build/installer/MicMap-Setup-v*.exe` > 1MB with complete stage tree)
- Task C verify block exits 0 OR is documented as skipped in SUMMARY (ISCC unavailable on this machine)
- ROADMAP.md Phase 4 Success Criterion #5 ("one build command yields one shippable installer") is demonstrably satisfied: a single `cmake --build build --target package` produces the .exe end-to-end
</verification>

<success_criteria>
- Four batch scripts deleted from repo; CONTEXT.md `<domain>` batch-script-deletion deliverable closed
- `cmake --build build --target package --config Release` produces `build/installer/MicMap-Setup-v{PROJECT_VERSION}.exe` on disk, > 1MB — ROADMAP Phase 4 Success Criterion #5 satisfied at the file-exists level
- Stage tree at `build/stage/` contains the D-01 layout: driver files under `drivers/micmap/`, app + manifest under `bin/`
- ISCC.exe exits 0 on the full composed `installer/MicMap.iss` (either via CMake wrapper in Task B or standalone in Task C) — INST-07 end-to-end closed
- Plans 01-08 are demonstrably composable: the package target assembles their outputs into one shippable artifact with no missing pieces
</success_criteria>

<output>
After completion, create `.planning/phases/04-installer/04-09-SUMMARY.md` capturing:
- Task A: `git status` output showing four batch-script deletions staged; grep-clause result (no CMake references)
- Task B: absolute path of produced .exe, file size in bytes, SHA256 hash, ISCC_EXECUTABLE path resolved by CMake, `build/stage/` final tree (`find build/stage -type f | sort`)
- Task C: ISCC standalone exit code; if skipped, reason (e.g., "Inno Setup not installed on this machine — CMake-wrapped ISCC via Task B is the signal")
- Any deviations from Plan 03's expected stage tree layout (e.g., a file appearing in build/stage/ that no install() rule produces — would indicate a mis-staged artifact to fix before VM UAT)
- Phase 4 close-out signal: all 7 INST requirements now closed at code + package-build level (INST-01/02/03/04/05 via Plans 04-08, INST-06 documented-as-voided-per-D-07 with defense-in-depth sweep retained, INST-07 via Plan 03 + this Plan 09); runtime validation is the VM UAT checklist in 04-VALIDATION.md §Manual-Only Verifications.
- Hand-off to `/gsd-verify-work 4`: the produced .exe path, SHA256, and "ready-for-UAT" marker.
</output>
</content>
</invoke>
