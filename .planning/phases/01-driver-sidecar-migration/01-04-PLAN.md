---
phase: 01-driver-sidecar-migration
plan: 04
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - apps/micmap/main.cpp
  - apps/hmd_button_test/main.cpp
  - src/steamvr/include/micmap/steamvr/dashboard_manager.hpp
  - src/steamvr/src/dashboard_manager.cpp
  - src/steamvr/CMakeLists.txt
autonomous: true
requirements: [SVR-07, SVR-08, SVR-11]
tags: [app, apps, state-machine, wiring, deletion]
must_haves:
  truths:
    - "apps/micmap/main.cpp no longer references IDashboardManager, createDashboardManager, or dashboardManager_; onTrigger is a two-branch function dispatching on PressEdge and calling driverClient->press()/release()."
    - "apps/hmd_button_test/main.cpp no longer references IDashboardManager or dashboard-state types; the test harness exposes manual Send Press / Send Release (or Tap) buttons that invoke driverClient->press()/release() hitting POST /button."
    - "src/steamvr/src/dashboard_manager.cpp and src/steamvr/include/micmap/steamvr/dashboard_manager.hpp are deleted from disk; src/steamvr/CMakeLists.txt no longer lists dashboard_manager.cpp."
    - "Grep sweep across driver/ src/ apps/ for the virtual-controller/dashboard-manager forbidden string set returns zero hits; build is clean under /WX (MSVC) on all targets."
    - "Full build succeeds: driver_micmap + micmap_core + micmap_steamvr + micmap + hmd_button_test + mic_test all link."
  artifacts:
    - path: "apps/micmap/main.cpp"
      provides: "Direct onTrigger(PressEdge) → driverClient press/release wiring"
      contains: "driverClient->press()"
      contains_2: "driverClient->release()"
      contains_3: "core::PressEdge"
    - path: "apps/hmd_button_test/main.cpp"
      provides: "Win32 GUI test harness with Send Press / Send Release buttons calling driverClient"
      contains: "driverClient->press()"
      contains_2: "driverClient->release()"
    - path: "src/steamvr/CMakeLists.txt"
      provides: "Updated sources list (no dashboard_manager.cpp)"
  key_links:
    - from: "apps/micmap/main.cpp state machine wiring"
      to: "IStateMachine::setTriggerCallback"
      via: "stateMachine->setTriggerCallback([this](core::PressEdge e) { onTrigger(e); });"
      pattern: "setTriggerCallback\\(\\[.*PressEdge"
    - from: "apps/micmap/main.cpp onTrigger"
      to: "IDriverClient::press / release"
      via: "if (edge == Down) driverClient->press(); else driverClient->release();"
      pattern: "driverClient->(press|release)\\(\\)"
    - from: "apps/hmd_button_test/main.cpp button handlers"
      to: "POST /button wire (via IDriverClient)"
      via: "driverClient_->press() / driverClient_->release()"
      pattern: "driverClient.*(press|release)"
---

<objective>
Complete the app-side rewire introduced by Plan 02: replace `onTrigger()` in `apps/micmap/main.cpp` with a two-branch `onTrigger(PressEdge)` that calls `driverClient->press()`/`release()`, repurpose `apps/hmd_button_test/main.cpp` buttons to hit the new `IDriverClient::press()`/`release()` surface, delete the `IDashboardManager` surface (`src/steamvr/src/dashboard_manager.cpp`, `src/steamvr/include/micmap/steamvr/dashboard_manager.hpp`), and trim `src/steamvr/CMakeLists.txt`. After this plan lands, the full build links (driver + core + steamvr + apps) and the forbidden-string grep sweep per SVR-07 returns zero hits across `driver/ src/ apps/`.

Purpose: Finish the phase's code side. This plan consumes the `PressEdge` callback contract from Plan 02 and the `POST /button` wire contract (sent by Plan 02's DriverClient impl, served by Plan 03's HttpServer). Plan 05 then performs the manual HMD validation spike (SVR-04, SVR-11).

Output: A fully-linking codebase with zero virtual-controller / dashboard-manager residue, with `hmd_button_test.exe` in its new shape ready for Plan 05's manual-VR session.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-driver-sidecar-migration/01-CONTEXT.md
@.planning/phases/01-driver-sidecar-migration/01-RESEARCH.md
@.planning/phases/01-driver-sidecar-migration/01-PATTERNS.md
@.planning/phases/01-driver-sidecar-migration/01-02-SUMMARY.md

<interfaces>
<!-- Contracts consumed by this plan (already landed in Wave 1 per Plan 02). -->

### From src/core/include/micmap/core/state_machine.hpp (Plan 02 Task 1)

```cpp
enum class PressEdge { Down, Up };
using TriggerCallback = std::function<void(PressEdge)>;

// Member: void setTriggerCallback(TriggerCallback cb);   // signature unchanged; argument type updated
```

### From src/steamvr/include/micmap/steamvr/vr_input.hpp (Plan 02 Task 2)

```cpp
class IDriverClient {
public:
    virtual bool connect() = 0;
    virtual void disconnect() = 0;
    virtual bool isConnected() const = 0;
    virtual bool press() = 0;      // POST /button {"state":"down"}
    virtual bool release() = 0;    // POST /button {"state":"up"}
    virtual bool getStatus() = 0;
    virtual int getPort() const = 0;
    virtual std::string getLastError() const = 0;
};
```

**Note:** `IDriverClient::click(std::string,int)`, `press(std::string)`, `release(std::string)` NO LONGER EXIST. Any caller still using them fails to compile — that's the intended driver forcing callsite rewrites in this plan.

### Forbidden-string set (SVR-07 grep gate)

After this plan lands, `grep -rnE '<regex>' driver/src src/ apps/` (excluding `.planning/` and `build/`) must return zero for EACH of:

```
VirtualController|virtual_controller
TrackedDeviceAdded
ProcessLauncher|process_launcher
IDashboardManager|DashboardManager|dashboard_manager
DashboardState|isDashboardOpen|dashboard_open|performDashboardAction
ControllerDevice|ITrackedDeviceServerDriver
open_vs_select
micmap_controller_profile
MICMAP_CONTROLLER_001
```
</interfaces>

### File ownership audit

Files this plan OWNS: `apps/micmap/main.cpp`, `apps/hmd_button_test/main.cpp`, `src/steamvr/src/dashboard_manager.cpp` (delete), `src/steamvr/include/micmap/steamvr/dashboard_manager.hpp` (delete), `src/steamvr/CMakeLists.txt`.

Non-overlap with Plan 03 confirmed: Plan 03 modifies `driver/**` and `src/steamvr/include/micmap/steamvr/vr_input.hpp` was already rewritten by Plan 02 (Wave 1). Plan 04 only READS vr_input.hpp, does not modify.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire apps/micmap/main.cpp — onTrigger(PressEdge) and delete IDashboardManager usage</name>
  <files>apps/micmap/main.cpp</files>
  <read_first>
    - apps/micmap/main.cpp (ALL — callsite inventory per PATTERNS §"apps/micmap/main.cpp (MODIFY)" lists lines 62 [dashboardManager field], 218 [driverClient factory], 230 [createDashboardManager], 241 [setTriggerCallback], 293, 334, 341-378 [onTrigger body], 390-393 [connection-status UI reads])
    - src/core/include/micmap/core/state_machine.hpp (verify the PressEdge + TriggerCallback shape landed by Plan 02)
    - src/steamvr/include/micmap/steamvr/vr_input.hpp (verify IDriverClient::press/release shape landed by Plan 02; also check whether DashboardState/HMDButtonAction/VREventType/VREvent/VREventCallback are still declared there — Plan 02 Task 2 may have removed them)
    - .planning/phases/01-driver-sidecar-migration/01-RESEARCH.md §"State-Machine Changes" callsite instructions (lines 605-609)
    - .planning/phases/01-driver-sidecar-migration/01-PATTERNS.md §"apps/micmap/main.cpp (MODIFY)" (lines 475-498)
    - .planning/phases/01-driver-sidecar-migration/01-CONTEXT.md §"App-side state machine" (D-12)
  </read_first>
  <behavior>
    - `onTrigger` signature is `void onTrigger(micmap::core::PressEdge edge)` (exact namespace per existing file's usage; if the file uses `using namespace` or `core::` prefix, match the idiom).
    - Body: if `driverClient` is null or not connected → return early (optionally log a debug line). If `edge == PressEdge::Down` → `driverClient->press();`. Else → `driverClient->release();`. Max 6 lines.
    - `setTriggerCallback` is invoked with a lambda of shape `[this](core::PressEdge e) { onTrigger(e); }`.
    - `dashboardManager` / `IDashboardManager` / `createDashboardManager` — completely removed from this file. Field declaration, initialization, update-tick calls, shutdown calls, all gone.
    - The MicMapApp no longer needs `#include <micmap/steamvr/dashboard_manager.hpp>` — remove the include line. If Plan 02 left `DashboardState`/`HMDButtonAction`/`VREvent` symbols in `vr_input.hpp`, this file should no longer reference them either.
    - Application init order unchanged: audio → detection → state machine → driver client. The state machine's `setTriggerCallback` wiring happens right after `createDriverClient` (so the callback's captured `driverClient` is non-null when edges fire).
    - Connection-status UI reads at lines ~390-393 (if they referenced `dashboardManager->getConnectionState()`) migrate to `driverClient->isConnected()` returning a bool suitable for the existing UI label.
    - The app continues to build as `apps/micmap/micmap.exe` (or whatever the current target name is per `apps/micmap/CMakeLists.txt`).
  </behavior>
  <action>
    Edit `apps/micmap/main.cpp` with these changes:

    1. **Remove includes:** delete any line that includes `<micmap/steamvr/dashboard_manager.hpp>` (the file may have a line like `#include "micmap/steamvr/dashboard_manager.hpp"` or angle-bracket form). Verify with `grep -n 'dashboard_manager' apps/micmap/main.cpp` before edit.

    2. **Remove field:** delete the `std::unique_ptr<steamvr::IDashboardManager> dashboardManager;` (or similar) declaration (around line 62 per PATTERNS). Also remove any `DashboardState` / `HMDButtonAction` type references if present.

    3. **Remove creation site:** delete the `dashboardManager = steamvr::createDashboardManager(...);` call (around line 230). If adjacent code set up callbacks or event subscriptions on dashboardManager (lines ~293), delete those blocks too.

    4. **Rewrite onTrigger signature:** change the method declaration (around line 103) from `void onTrigger();` to `void onTrigger(core::PressEdge edge);` (use `micmap::core::PressEdge` if that's the resolution in this file — match existing namespace aliases).

    5. **Rewrite onTrigger body:** REPLACE the entire body (currently ~lines 341-378, the three-layer fallback) with:
       ```cpp
       void MicMapApp::onTrigger(core::PressEdge edge) {
           if (!driverClient || !driverClient->isConnected()) {
               MICMAP_LOG_DEBUG("onTrigger({}): driver not connected, skipping",
                                edge == core::PressEdge::Down ? "down" : "up");
               return;
           }
           bool ok = (edge == core::PressEdge::Down) ? driverClient->press()
                                                     : driverClient->release();
           if (!ok) {
               MICMAP_LOG_WARN("onTrigger failed: {}", driverClient->getLastError());
           }
       }
       ```
       Remove the `performDashboardAction`, `isDashboardOpen`, `DashboardState`, and `HMDButtonAction` references entirely. Do NOT leave any fallback path — there's no "fallback" to take since the virtual controller is gone.

    6. **Fix setTriggerCallback wiring:** the line around 241 currently reads something like `stateMachine->setTriggerCallback([this]() { onTrigger(); });`. Change to:
       ```cpp
       stateMachine->setTriggerCallback([this](core::PressEdge edge) { onTrigger(edge); });
       ```

    7. **Fix shutdown path:** if lines around 334 have `if (dashboardManager) dashboardManager->stop();` or similar, delete those lines. The shutdown path becomes: stop state machine → disconnect driver client → stop detection → stop audio.

    8. **Fix connection-status UI reads:** if lines around 390-393 show something like `auto state = dashboardManager->getConnectionState();` followed by a UI label update, REPLACE with:
       ```cpp
       bool connected = driverClient && driverClient->isConnected();
       // existing UI label update, using `connected` bool instead of DashboardState
       ```
       If the UI previously displayed a DashboardState enum name (e.g. "Open"/"Closed"/"Unknown"), replace with a binary "Connected"/"Disconnected" label. Acceptable.

    9. **Namespace check:** verify all `core::PressEdge` references resolve. If the file uses `namespace micmap { namespace core { ... } }` or aliases `using micmap::core::...`, match the idiom exactly. If the file says `#include <micmap/core/state_machine.hpp>`, no change to the include is needed — Plan 02 already updated that header.

    Build verification: `cmake --build build --config Debug --target micmap` must succeed with zero warnings. If the project has `/WX` enabled project-wide (likely not, per CMakeLists.txt:46,56 currently), any warning fails the build. Clean all warnings as part of this task.
  </action>
  <verify>
    <automated>cd C:/Users/decid/Documents/projects/mic-map && ! grep -q 'dashboard_manager' apps/micmap/main.cpp && ! grep -q 'IDashboardManager' apps/micmap/main.cpp && ! grep -q 'createDashboardManager' apps/micmap/main.cpp && ! grep -q 'performDashboardAction' apps/micmap/main.cpp && ! grep -q 'DashboardState' apps/micmap/main.cpp && grep -q 'driverClient->press()' apps/micmap/main.cpp && grep -q 'driverClient->release()' apps/micmap/main.cpp && grep -qE 'onTrigger\(.*PressEdge' apps/micmap/main.cpp && grep -qE 'setTriggerCallback\(\[this\].*PressEdge' apps/micmap/main.cpp && cmake --build build --config Debug --target micmap</automated>
  </verify>
  <acceptance_criteria>
    - `apps/micmap/main.cpp` does NOT contain: `dashboard_manager`, `IDashboardManager`, `createDashboardManager`, `performDashboardAction`, `DashboardState`, `isDashboardOpen`.
    - `apps/micmap/main.cpp` contains: `driverClient->press()`, `driverClient->release()`, `onTrigger(` with a `PressEdge` parameter, `setTriggerCallback([this]` with a `PressEdge` parameter in the lambda.
    - `grep -c 'driverClient->press()' apps/micmap/main.cpp` returns exactly `1` (single call site in onTrigger). Same for `driverClient->release()` — exactly `1`.
    - `cmake --build build --config Debug --target micmap` succeeds.
    - No new warnings introduced (compare warning counts before/after if feasible; otherwise: build log shows zero warnings for apps/micmap).
  </acceptance_criteria>
  <done>
    - `apps/micmap/main.cpp` compiles and links.
    - `onTrigger(PressEdge)` is the single integration point between state machine and driver client.
    - All IDashboardManager residue is gone from this file.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repurpose apps/hmd_button_test/main.cpp as POST /button test harness</name>
  <files>apps/hmd_button_test/main.cpp</files>
  <read_first>
    - apps/hmd_button_test/main.cpp (ALL — PATTERNS §"apps/hmd_button_test/main.cpp (MODIFY)" identifies the edit zones: lines 29-30, 60-61 [field decls], 164-271 [dashboard manager init/tick block], 605-838 [button handlers including "Send Click" at 636-672, "Open Dashboard" at 608-630, "Click A" at 769-803, "Click Trigger" at 806+, connection-state labels]; lines 730-757 [HTTP /health probe — KEEP])
    - src/steamvr/include/micmap/steamvr/vr_input.hpp (IDriverClient::press/release landed by Plan 02)
    - .planning/phases/01-driver-sidecar-migration/01-RESEARCH.md §"State of the Art" (lines 812-820 — "open vs select" is the old split; new model is press/release edges)
    - .planning/phases/01-driver-sidecar-migration/01-CONTEXT.md §D-13 — use harness as-is for the spike; single-tap convenience button is acceptable
    - .planning/phases/01-driver-sidecar-migration/01-PATTERNS.md §"apps/hmd_button_test/main.cpp (MODIFY)" (lines 501-515)
    - apps/hmd_button_test/CMakeLists.txt (identify the target name — typically `hmd_button_test`)
  </read_first>
  <behavior>
    - Harness still compiles as a Win32 GUI executable (`hmd_button_test.exe`). WinMain / message pump / window creation unchanged.
    - `IDashboardManager` field and all its lifecycle calls — removed.
    - Connection indicator: "SteamVR: Connected/Disconnected" label driven by `driverClient_->isConnected()` rather than any dashboard-state.
    - Three new/rewired buttons (or their direct equivalents — button names may vary based on existing layout):
      1. **"Send Press (Down)"** — calls `driverClient_->press()`. Shows success/failure in status area.
      2. **"Send Release (Up)"** — calls `driverClient_->release()`. Shows success/failure in status area.
      3. **"Tap (Press + 150ms + Release)"** — convenience for operator: calls `press()`, `Sleep(150)`, `release()`. This is the D-13 operator-friendly single-click.
    - OLD buttons to DELETE: "Open Dashboard", "Send Click" (the `click("trigger", 100)` form), "Click A", "Click Trigger" — all virtual-controller-specific. Just delete their handlers and remove their `CreateWindowEx(... "BUTTON" ...)` lines from the window-creation block.
    - Optional KEEP: any "Test Driver" / "Connect Driver" button that just calls `driverClient_->connect()` + reports result. Simplifies operator workflow during Plan 05 spike.
    - No code depends on `IDashboardManager` or dashboard-state types. If Plan 02 left `DashboardState`/`HMDButtonAction`/etc. in `vr_input.hpp`, this file does not reference them.
    - HTTP `/health` probe loop (lines ~730-757) — KEEP unchanged. It drives the port-discovery.
    - Build: `cmake --build build --config Debug --target hmd_button_test` succeeds cleanly.
  </behavior>
  <action>
    Edit `apps/hmd_button_test/main.cpp`:

    1. **Remove includes:** delete `#include <micmap/steamvr/dashboard_manager.hpp>` (if present; likely at top of file).

    2. **Remove field declarations (around lines 29-30, 60-61):** delete `std::unique_ptr<micmap::steamvr::IDashboardManager> dashboardManager_;` and any `DashboardState state_` enum member if present.

    3. **Remove init/lifecycle block (around lines 164-271):** delete the whole `dashboardManager_ = steamvr::createDashboardManager(...)` setup, any callback registration on dashboardManager_, any per-frame `dashboardManager_->update()` call (if the harness has an update tick), and any shutdown call `dashboardManager_->stop()`.

    4. **Rewrite button handlers (around lines 605-838):**
       - Locate the "Open Dashboard" button handler (~608-630). DELETE the entire `case IDM_OPEN_DASHBOARD:` block (or whatever control ID it uses). Also DELETE the corresponding `CreateWindowExW(..., "BUTTON", L"Open Dashboard", ...)` line in the window-creation block.
       - Locate "Send Click" handler (~636-672, calls `driverClient->click("trigger", 100)`). DELETE and replace with NEW handlers:
         ```cpp
         case IDM_SEND_PRESS: {
             if (driverClient_ && driverClient_->press()) {
                 AppendStatus(L"press() OK\r\n");
             } else {
                 AppendStatus(L"press() FAILED: ");
                 AppendStatusUtf8(driverClient_ ? driverClient_->getLastError() : "no client");
             }
             break;
         }
         case IDM_SEND_RELEASE: {
             if (driverClient_ && driverClient_->release()) {
                 AppendStatus(L"release() OK\r\n");
             } else {
                 AppendStatus(L"release() FAILED: ");
                 AppendStatusUtf8(driverClient_ ? driverClient_->getLastError() : "no client");
             }
             break;
         }
         case IDM_SEND_TAP: {
             if (!driverClient_) { AppendStatus(L"Tap FAILED: no client\r\n"); break; }
             bool down = driverClient_->press();
             ::Sleep(150);
             bool up = driverClient_->release();
             if (down && up) AppendStatus(L"Tap OK\r\n");
             else            AppendStatus(L"Tap FAILED\r\n");
             break;
         }
         ```
         (Adjust `AppendStatus` / `AppendStatusUtf8` to match whatever status-write helper already exists in this file — grep for existing `AppendText`/`SetWindowText`/similar patterns.)
       - Locate "Click A" (~769-803) and "Click Trigger" (~806+) handlers. DELETE both blocks in full (virtual-controller-specific; no replacement needed).
       - Add new control IDs: `IDM_SEND_PRESS`, `IDM_SEND_RELEASE`, `IDM_SEND_TAP` — pick numeric values that don't collide with existing IDs (e.g. 2001, 2002, 2003 if the existing IDs are in the 1000-1500 range).
       - In the window-creation block (wherever the old buttons were `CreateWindowEx`'d), ADD three new button creations:
         ```cpp
         CreateWindowW(L"BUTTON", L"Send Press (Down)",
             WS_VISIBLE | WS_CHILD | BS_PUSHBUTTON,
             x, y, btnW, btnH, hwnd, (HMENU)IDM_SEND_PRESS, hInstance, NULL);
         CreateWindowW(L"BUTTON", L"Send Release (Up)",
             WS_VISIBLE | WS_CHILD | BS_PUSHBUTTON,
             x + btnW + 10, y, btnW, btnH, hwnd, (HMENU)IDM_SEND_RELEASE, hInstance, NULL);
         CreateWindowW(L"BUTTON", L"Tap (Press + 150ms + Release)",
             WS_VISIBLE | WS_CHILD | BS_PUSHBUTTON,
             x + 2*(btnW + 10), y, btnW + 80, btnH, hwnd, (HMENU)IDM_SEND_TAP, hInstance, NULL);
         ```
         (Position coordinates must use whatever layout variables already exist in the file — match the idiom of the buttons being replaced.)

    5. **Connection-status label:** if the file has a label that shows "Dashboard: Open/Closed/Unknown", change the update logic to show "SteamVR: Connected" or "SteamVR: Disconnected" based on `driverClient_->isConnected()`. Delete any code computing a DashboardState.

    6. **KEEP unchanged:** WinMain, message pump, `/health` probe at lines ~730-757, `driverClient_ = createDriverClient(...)` initialization (if it exists; otherwise add it where dashboardManager was previously initialized).

    7. **Verification of NO-stale-symbols:** grep the file after edit — `grep -nE 'dashboard_manager|IDashboardManager|createDashboardManager|DashboardState|performDashboardAction|isDashboardOpen|HMDButtonAction|click\(.*trigger' apps/hmd_button_test/main.cpp` returns empty. Also no `driverClient_->click(` pattern.

    Build verification: `cmake --build build --config Debug --target hmd_button_test`. Must succeed. On MSVC without /WX on this target (WX is only driver-wide from Plan 03), warnings are non-blocking but should be zero anyway.
  </action>
  <verify>
    <automated>cd C:/Users/decid/Documents/projects/mic-map && ! grep -q 'dashboard_manager' apps/hmd_button_test/main.cpp && ! grep -q 'IDashboardManager' apps/hmd_button_test/main.cpp && ! grep -q 'DashboardState' apps/hmd_button_test/main.cpp && ! grep -q 'performDashboardAction' apps/hmd_button_test/main.cpp && ! grep -qE 'driverClient_?->click\(' apps/hmd_button_test/main.cpp && grep -qE 'driverClient_?->press\(\)' apps/hmd_button_test/main.cpp && grep -qE 'driverClient_?->release\(\)' apps/hmd_button_test/main.cpp && grep -q 'Send Press' apps/hmd_button_test/main.cpp && grep -q 'Send Release' apps/hmd_button_test/main.cpp && cmake --build build --config Debug --target hmd_button_test && test -f build/apps/hmd_button_test/Debug/hmd_button_test.exe -o -f build/apps/hmd_button_test/hmd_button_test.exe</automated>
  </verify>
  <acceptance_criteria>
    - `apps/hmd_button_test/main.cpp` does NOT contain: `dashboard_manager`, `IDashboardManager`, `DashboardState`, `performDashboardAction`, `driverClient_->click(`, `driverClient->click(`.
    - `apps/hmd_button_test/main.cpp` contains literal strings: `driverClient_->press()` (or variant), `driverClient_->release()`, `Send Press`, `Send Release`, `Tap` (button label for the convenience control).
    - `cmake --build build --config Debug --target hmd_button_test` succeeds.
    - Binary produced at either `build/apps/hmd_button_test/Debug/hmd_button_test.exe` or `build/apps/hmd_button_test/hmd_button_test.exe` (depending on single-config vs multi-config generator).
    - Window layout changes are visual only — Plan 05 will verify operator usability manually.
  </acceptance_criteria>
  <done>
    - Test harness compiles with new button set.
    - Buttons invoke `IDriverClient::press/release/tap` hitting POST /button (assuming driver is running).
    - Dashboard-manager residue is gone.
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete dashboard_manager files + trim src/steamvr/CMakeLists.txt + run forbidden-string grep sweep</name>
  <files>src/steamvr/src/dashboard_manager.cpp, src/steamvr/include/micmap/steamvr/dashboard_manager.hpp, src/steamvr/CMakeLists.txt</files>
  <read_first>
    - src/steamvr/src/dashboard_manager.cpp (read before deletion to confirm no external dependency missed)
    - src/steamvr/include/micmap/steamvr/dashboard_manager.hpp (same)
    - src/steamvr/CMakeLists.txt (current sources list — identify the line with `src/dashboard_manager.cpp`, typically line 14)
    - .planning/phases/01-driver-sidecar-migration/01-RESEARCH.md §"Deletion Blast-Radius Map" (lines 684-731) — canonical forbidden-string set at lines 719-730
    - .planning/phases/01-driver-sidecar-migration/01-PATTERNS.md §"Files to DELETE" (lines 34-43)
    - .planning/phases/01-driver-sidecar-migration/01-CONTEXT.md §D-14
    - Previous task outputs: Task 1 and Task 2 must be COMPLETE before this task runs (files they modified must no longer reference dashboard_manager — otherwise the delete breaks the build).
  </read_first>
  <action>
    **Part A — Delete.** Remove both files in full:
    - `src/steamvr/src/dashboard_manager.cpp`
    - `src/steamvr/include/micmap/steamvr/dashboard_manager.hpp`

    **Part B — Trim src/steamvr/CMakeLists.txt.** Open the file. Find the `add_library` or `target_sources` block that lists `src/dashboard_manager.cpp` (per PATTERNS §line 14). DELETE that single line. If the list becomes awkward (trailing comma, orphan indentation), tidy formatting.

    **Part C — Forbidden-string grep sweep (SVR-07 gate).** Run ONCE, after Parts A and B:
    ```
    grep -rnE 'VirtualController|virtual_controller|TrackedDeviceAdded|ProcessLauncher|process_launcher|IDashboardManager|DashboardManager|dashboard_manager|DashboardState|isDashboardOpen|dashboard_open|performDashboardAction|ControllerDevice|ITrackedDeviceServerDriver|open_vs_select|micmap_controller_profile|MICMAP_CONTROLLER_001' driver/src src/ apps/
    ```
    (Excluding `.planning/` and `build/` paths — these are not searched. Excluding `tests/` unless tests reference them, which they shouldn't.)

    Expected result: ZERO hits.

    If ANY hits remain:
    - If the hit is in a comment / docstring in a file you own: remove it.
    - If the hit is in a file you do NOT own (outside this plan's `files_modified`): FAIL the task and raise to the orchestrator. Some unknown dependency needs addressing before the phase can close.
    - If the hit is in `.planning/` or `build/`: the grep should have excluded those — re-run with `--exclude-dir='.planning' --exclude-dir=build` to confirm.

    **Part D — Optional bonus:** If `vr_input.hpp` still declares `DashboardState`, `HMDButtonAction`, `VREventType`, `VREvent`, `VREventCallback` (because Plan 02 Task 2 deferred their removal pending Plan 04 callsite cleanup), and no file in `apps/ src/ driver/` now references them (grep confirms), delete those type declarations from `vr_input.hpp`. Otherwise leave them and note in SUMMARY.

    **Part E — Full build verification.** Run a clean full build:
    ```
    cmake --build build --config Debug
    ```
    ALL targets must link: `driver_micmap`, `micmap_core`, `micmap_steamvr`, `micmap` (the app), `hmd_button_test`, `mic_test` (if present), `test_command_queue`, `test_placeholder`.

    Then run CTest:
    ```
    ctest --test-dir build --output-on-failure -C Debug
    ```
    Both `test_placeholder` and `test_command_queue` must Pass.

    **Part F — Post-build final grep.** Ensure the installed output doesn't contain ghost artifacts:
    ```
    find build/driver/micmap/resources -name 'micmap_controller_profile.json' -o -name 'vrcompositor_bindings_micmap_controller.json'
    ```
    Must return empty.
  </action>
  <verify>
    <automated>cd C:/Users/decid/Documents/projects/mic-map && ! test -f src/steamvr/src/dashboard_manager.cpp && ! test -f src/steamvr/include/micmap/steamvr/dashboard_manager.hpp && ! grep -q 'dashboard_manager.cpp' src/steamvr/CMakeLists.txt && [ "$(grep -rnE 'VirtualController|virtual_controller|TrackedDeviceAdded|ProcessLauncher|process_launcher|IDashboardManager|DashboardManager|dashboard_manager|DashboardState|isDashboardOpen|dashboard_open|performDashboardAction|ControllerDevice|ITrackedDeviceServerDriver|open_vs_select|micmap_controller_profile|MICMAP_CONTROLLER_001' driver/src src/ apps/ 2>/dev/null | wc -l)" = "0" ] && cmake --build build --config Debug && ctest --test-dir build --output-on-failure -C Debug && [ -z "$(find build/driver/micmap/resources -name 'micmap_controller_profile.json' -o -name 'vrcompositor_bindings_micmap_controller.json' 2>/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
    - Both dashboard_manager files absent from the working tree.
    - `src/steamvr/CMakeLists.txt` does not contain `dashboard_manager.cpp`.
    - Forbidden-string grep across `driver/src`, `src/`, `apps/` returns exactly 0 hits.
    - Full build succeeds: `cmake --build build --config Debug` with no error.
    - All CTest tests Pass.
    - No resource files `micmap_controller_profile.json` or `vrcompositor_bindings_micmap_controller.json` under `build/driver/micmap/resources/`.
  </acceptance_criteria>
  <done>
    - `IDashboardManager` surface fully eliminated from the project.
    - SVR-07 grep gate passes (zero forbidden-string hits).
    - Full build green; unit tests green.
    - Phase 1 code is structurally complete; only manual HMD validation (Plan 05) remains.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App process ↔ driver DLL (localhost HTTP) | Same boundary as Plan 02 / Plan 03 — the wire format is now `POST /button` with JSON body. No change in trust model; this plan only rewires which code calls the client and which test harness exercises it. |
| Test harness ↔ app contract | `hmd_button_test.exe` is a developer tool, not shipped to end users. It creates an `IDriverClient` instance and drives it manually. No privileged operation beyond what the main app does. |
| Human operator ↔ test harness | Operator launches `hmd_button_test.exe` on a SteamVR-equipped dev box. Clicks buttons. Observes dashboard behavior. Clicking "Send Press" without "Send Release" strands the button for 5s before the driver's max-hold watchdog (Plan 03) auto-releases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering — app sends an incomplete press/release pair | `onTrigger(PressEdge)` | mitigate | The state machine (Plan 02) is the sole source of `PressEdge` values in the app. Its Releasing→Cooldown transition guarantees a Down is always followed by an Up unless the process crashes. Process crash mid-press is handled driver-side by the max-hold watchdog (Plan 03). |
| T-04-02 | Denial of Service via test harness — operator spams "Send Press" without "Send Release" | `hmd_button_test` button handlers | accept | Only a developer with filesystem access to the dev box can run `hmd_button_test.exe`. Worst-case: button stays down for 5s until driver watchdog fires. No data corruption, no privilege escalation, no crash. Known behavior; documented for the spike operator. |
| T-04-03 | Elevation of Privilege — hmd_button_test invokes something that main app can't | `hmd_button_test` | accept | `hmd_button_test` uses the SAME `IDriverClient` factory as the main app. Every HTTP call it makes is also callable from `micmap.exe`. Zero additional privilege. |
| T-04-04 | Information Disclosure — MICMAP_LOG_WARN in onTrigger leaks driver error details | `onTrigger` | accept | `driverClient->getLastError()` returns strings like `"Server returned status 400"` — these are diagnostic, not sensitive. Logs go to MicMap's own log file (user-scoped). Same disclosure profile as the pre-phase logging. |
| T-04-05 | Spoofing — removed IDashboardManager had no auth gate; new direct-press path also has none | app wiring | accept (inherits Plan 03 threat model) | Removing IDashboardManager does not reduce security posture — it had no auth either. POST /button server-side threat model lives in Plan 03 (T-03-01 / T-03-02). This plan only changes the caller. |

No new HIGH-severity threats introduced by this plan. All items are either managed by Plan 02/03 mitigations (the wire format / server hardening) or accepted as out-of-scope for ASVS L1 (local developer tooling).
</threat_model>

<verification>
- `apps/micmap/main.cpp` and `apps/hmd_button_test/main.cpp` grep-verified free of dashboard-manager / virtual-controller residue.
- `src/steamvr/src/dashboard_manager.cpp` and `.hpp` absent from disk.
- `src/steamvr/CMakeLists.txt` no longer references `dashboard_manager.cpp`.
- Full forbidden-string sweep across `driver/src src/ apps/` returns zero hits.
- `cmake --build build --config Debug` succeeds for all targets.
- `ctest --test-dir build --output-on-failure -C Debug` shows `test_command_queue` and `test_placeholder` both Passed.
- No controller-profile JSONs in `build/driver/micmap/resources/input/`.
</verification>

<success_criteria>
1. `apps/micmap/main.cpp` has a two-line `onTrigger(PressEdge)` body that press()/release()-es the driver client based on the edge; no dashboard-manager fallback.
2. `apps/hmd_button_test/main.cpp` exposes Send Press / Send Release / Tap buttons; no virtual-controller-specific buttons or handlers remain.
3. Dashboard-manager cpp/hpp pair deleted; CMakeLists.txt trimmed.
4. SVR-07 forbidden-string grep sweep returns zero hits across `driver/src src/ apps/`.
5. Full Debug build succeeds for every target in the project.
6. CTest green (test_command_queue + test_placeholder).
</success_criteria>

<output>
Create `.planning/phases/01-driver-sidecar-migration/01-04-SUMMARY.md` capturing:
- Diff summary per file (key lines added/removed).
- Before/after for the onTrigger body and setTriggerCallback wiring in apps/micmap/main.cpp.
- Button-rewire summary for apps/hmd_button_test/main.cpp (old buttons deleted, new buttons added).
- Confirmation of dashboard_manager file deletion (with `git status` snapshot).
- Full forbidden-string grep command + output (must be empty).
- `cmake --build build --config Debug` last-line success indicator.
- `ctest --output-on-failure -C Debug` output showing both tests Passed.
- Decision on the Plan 02-deferred `DashboardState`/`HMDButtonAction`/`VREventType`/`VREvent`/`VREventCallback` symbols in `vr_input.hpp` — kept or removed, with reason.
- Ready-for-spike statement: `hmd_button_test.exe` built, ready for Plan 05 manual-VR session.
</output>
